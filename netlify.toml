[build]
  command = "npm run build:netlify"
  publish = ".next"

[build.environment]
  NODE_VERSION = "22.13.0"

# Source-upload deploys do not receive Netlify's automatic framework injection,
# so opt in explicitly while leaving the adapter version managed by Netlify.
[[plugins]]
  package = "@netlify/plugin-nextjs"
